{
  "name": "edgetunnel",
  "version": "1.0.0",
  "main": "src/worker.js",
  "scripts": {
    "test": "jest",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/wrangler": "^1.0.0"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
